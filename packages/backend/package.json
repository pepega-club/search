{
	"name": "backend",
	"version": "1.0.0",
	"main": "dist/index.js",
	"scripts": {
		"dev": "concurrently -k \"tsc -w\" \"nodemon dist\"",
		"start": "node dist/index.js",
		"build": "tsc",
		"build:watch": "tsc -w",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"tsconfig": "workspace:^0.0.1"
	}
}
