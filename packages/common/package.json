{
	"name": "common",
	"version": "1.0.0",
	"main": "dist/index.js",
	"scripts": {
		"build": "tsc",
		"build:watch": "tsc -w",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"tsconfig": "workspace:^0.0.1"
	}
}
